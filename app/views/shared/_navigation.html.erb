<%= link_to 'Test-Guru', tests_path %>

<% if logged_in? %>
  <p class="nav user">Welcome, <b><%= current_user.mail %></b> Guru | <%= link_to 'Log out', session_path(session), method: :delete %></p>
<% else %>
  <p class="nav user"><%= link_to 'Login', login_path %> | <%= link_to 'Register', signup_path %> </p>
<% end %>
